/**
 * Empty and minimal function bodies.
 * Verifies these are still tracked in coverage.
 */

export function emptyVoid(): void {}

export function returnsZero(): i32 {
  return 0;
}

export function nonEmpty(x: i32): i32 {
  return x * 2;
}
