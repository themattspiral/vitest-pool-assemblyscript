// Const-only module parity twin (v8 oracle) — mirrors the AS fixture
// coverage-collection/statement/module-consts.meta.ts line-for-line. Compared
// in meta-verify/coverage-collection/statement/module-consts.test.ts. KEEP
// LINE-ALIGNED (this header matches the AS fixture's line count — do not
// change the line count).

export const TABLE_SIZE = 1024;
export const MAX_RETRIES = 3;
export const ENABLED = true;
