{
  "compilerOptions": {
    // Target Node.js 18+ (ES2022 features)
    "target": "ES2022",
    "lib": ["ES2022", "DOM"],  // DOM needed for WebAssembly types (TS limitation)

    // ESM module format for Node.js (matches package.json "type": "module")
    "module": "NodeNext",
    "moduleResolution": "NodeNext",

    // Output configuration (for reference - tsup handles actual builds)
    "rootDir": "./",
    "outDir": "./dist",
    "declaration": true,
    "declarationMap": true,
    "sourceMap": true,

    // Strict type checking
    "types": ["node"],
    "strict": true,
    "noUncheckedIndexedAccess": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "noImplicitReturns": true,
    "noFallthroughCasesInSwitch": true,

    // Module compatibility
    "esModuleInterop": false,
    "allowSyntheticDefaultImports": true,
    "resolveJsonModule": true,
    "isolatedModules": true,
    "isolatedDeclarations": true,

    // Performance & compatibility
    "skipLibCheck": true,
    "forceConsistentCasingInFileNames": true
  },

  "include": [
    "src/**/*.ts",
    "test/**/*.ts"
  ],

  "exclude": [
    "node_modules",
    "dist",
    "build",
    "assembly",
    "test/assembly/**/*",
    "test/assembly-src/**/*",
    "test-external/**/*",
  ]
}
